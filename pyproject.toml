[project]
name = "netcrawl-workspace"
version = "0.1.0"
description = "Starter workspace for NetCrawl — write Python workers to automate your network."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "netcrawl",
]
